# Ejemplo de variables de entorno para Railway
# Copia este archivo a .env (local) o define las variables en Railway Dashboard

# Django
SECRET_KEY=tu_secret_key_aqui
DEBUG=False
ALLOWED_HOSTS=tu-dominio.com,localhost,*

# Database (Railway define DATABASE_URL automáticamente)
# DATABASE_URL=postgres://user:password@host:port/dbname

# Seguridad y HSTS
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=2592000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True
